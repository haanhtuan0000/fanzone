import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0D1117);
  static const neonGreen = Color(0xFF39FF14);
  static const amber = Color(0xFFFFB800);
  static const red = Color(0xFFFF4444);
  static const blue = Color(0xFF4A9EFF);
  static const cardSurface = Color(0xFF161B22);
  static const cardSurfaceLight = Color(0xFF1C2333);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8B949E);
  static const divider = Color(0xFF30363D);
  static const gold = Color(0xFFFFD700);
  static const silver = Color(0xFFC0C0C0);
  static const bronze = Color(0xFFCD7F32);
  static const purple = Color(0xFF8B5CF6);
}

class AppFonts {
  static const bebasNeue = 'BebasNeue';
  static const barlowCondensed = 'BarlowCondensed';
}

class AppDurations {
  static const quick = Duration(milliseconds: 200);
  static const normal = Duration(milliseconds: 350);
  static const slow = Duration(milliseconds: 500);
  static const countdown = Duration(seconds: 30);
}

class AppSizes {
  static const double cardRadius = 12.0;
  static const double buttonRadius = 8.0;
  static const double bottomNavHeight = 72.0;
  static const double scoreFontSize = 36.0;
  static const double clockFontSize = 18.0;
}
